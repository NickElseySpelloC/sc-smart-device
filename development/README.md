# Development folder

> TO DO: Convert files in this folder to work with the new sc-smart-device object model.

This folder contains small Python apps used to perform mid-development tests of the classes 
in this library. See the /tests folder for pytest unit tests.
